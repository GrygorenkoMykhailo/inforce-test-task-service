apiVersion: v1
kind: Service
metadata:
  name: go-service
spec:
  selector:
    app: go-service
  ports:
    - protocol: TCP
      port: {{ .Values.goService.port }}
      targetPort: {{ .Values.goService.port }}